function searchDatabase(){
    // get input value
    let inputVal = document.getElementsByClassName("inputId")[0].value;
    // display inputVal
    alert(inputVal);
    // get place value in table
    // window.location.href = '#statsTableHolder';
}